const { Given, When, Then } = require('@cucumber/cucumber');

Given('I have a configured application', function () {
    // Setup code here
});

When('I perform an action', function () {
    // Action code here
});

Then('I should see the expected result', function () {
    // Assertion code here
});